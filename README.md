TestingWikistack Workshop README

For this workshop, we use the mocha.js testing framework with chai.js for assertions. We write our tests just like any other node code, only instead of building features, we build tests that probe those features.